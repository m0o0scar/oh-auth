## 1. Implementation
- [ ] 1.1 Add POST handling for `/secret/encrypt` that accepts JSON `url` and `password` and returns the decrypt URL in JSON.
- [ ] 1.2 Implement validation/error responses for missing `url` or `password` in POST requests.
- [ ] 1.3 Add or update tests and documentation to cover the POST encryption flow.
- [ ] 1.4 Run `openspec validate add-post-secret-encrypt-api --strict`.

